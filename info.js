const canvas=document.createElement('canvas');canvas.className='info-wallpaper';canvas.setAttribute('aria-hidden','true');document.body.prepend(canvas);const context=canvas.getContext('2d'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let dots=[],raf;function resize(){const d=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*d;canvas.height=innerHeight*d;context.setTransform(d,0,0,d,0,0);dots=Array.from({length:Math.min(75,Math.max(30,innerWidth/20))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16,b:Math.random()>.72}))}function draw(){context.clearRect(0,0,innerWidth,innerHeight);dots.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;context.fillStyle=p.b?'#4c9aff88':'#baff2977';context.beginPath();context.arc(p.x,p.y,1.2,0,7);context.fill()});for(let i=0;i<dots.length;i++)for(let j=i+1;j<dots.length;j++){const a=dots[i],b=dots[j],dist=Math.hypot(a.x-b.x,a.y-b.y);if(dist<120){context.strokeStyle=`rgba(130,210,85,${(1-dist/120)*.11})`;context.beginPath();context.moveTo(a.x,a.y);context.lineTo(b.x,b.y);context.stroke()}}if(!reduced)raf=requestAnimationFrame(draw)}addEventListener('resize',resize,{passive:true});document.addEventListener('visibilitychange',()=>{cancelAnimationFrame(raf);if(!document.hidden)draw()});resize();draw();
