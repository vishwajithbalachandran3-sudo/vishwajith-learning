const applicationForm=document.querySelector('#application-form'),roleField=document.querySelector('#role'),resumeField=document.querySelector('#resume');roleField.value=new URLSearchParams(location.search).get('role')||'';applicationForm.addEventListener('submit',async event=>{event.preventDefault();const button=applicationForm.querySelector('button'),status=applicationForm.querySelector('.apply-status'),file=resumeField.files[0];if(!file)return;if(file.size>10*1024*1024){status.textContent='Please choose a résumé smaller than 10 MB.';return}const allowed=['application/pdf','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document'];if(file.type&&!allowed.includes(file.type)){status.textContent='Please upload a PDF, DOC, or DOCX file.';return}const formData=new FormData(applicationForm);if(formData.get('_honey'))return;formData.append('_replyto',formData.get('email'));formData.append('_subject',`Career application — ${formData.get('role')}`);formData.append('_template','table');formData.append('source',location.href);button.disabled=true;button.textContent='Uploading résumé…';status.textContent='';try{const response=await fetch('https://formsubmit.co/ajax/info@thecybersol.com',{method:'POST',headers:{Accept:'application/json'},body:formData}),data=await response.json();if(!response.ok)throw new Error(data.message||'Unable to send');status.textContent=data.message||'Application and résumé sent. Thank you for your interest.';if(data.success!=='false'&&data.success!==false)applicationForm.reset()}catch(error){status.textContent='We could not send your application. Please try again later.'}finally{button.disabled=false;button.textContent='Send application →'}});
