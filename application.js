const applicationForm=document.querySelector('#application-form'),roleField=document.querySelector('#role');roleField.value=new URLSearchParams(location.search).get('role')||'';applicationForm.addEventListener('submit',async event=>{event.preventDefault();const button=applicationForm.querySelector('button'),status=applicationForm.querySelector('.apply-status'),payload=Object.fromEntries(new FormData(applicationForm));if(payload._honey)return;button.disabled=true;button.textContent='Sending…';status.textContent='';try{const response=await fetch('https://formsubmit.co/ajax/info@thecybersol.com',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({...payload,_replyto:payload.email,_subject:`Career application — ${payload.role}`,source:location.href})}),data=await response.json();if(!response.ok)throw new Error(data.message||'Unable to send');status.textContent=data.message||'Application sent. Thank you for your interest.';if(data.success!=='false'&&data.success!==false)applicationForm.reset()}catch(error){status.textContent='We could not send your application. Please email info@thecybersol.com.'}finally{button.disabled=false;button.textContent='Send application →'}});
